# CoEdit2
CoEdit is a real-time collaborative document editor built with Spring Boot that uses CRDTs to handle concurrent edits without locking. It guarantees eventual consistency across users, supports low-latency incremental synchronization, and demonstrates practical distributed systems and concurrency control design.
